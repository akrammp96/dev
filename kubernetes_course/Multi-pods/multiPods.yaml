apiVersion: v1
kind: Pod
metadata:
  name: yellow
  labels: 
   name: yellow
spec:     
  containers:
  - name: lemon
    image: busybox
    ports: 
     - containerPort: 8080
    volumeMounts:
    - mountPath: /var/log/event-simulator
      name: log-volume
  - name: gold
    image: redis
    volumeMounts:
    - mountPath: /log
      name: log-volume



see logs inside container with:

k logs app -n elastic-stack

k -n elastic-stack exec -it app --cat /log/app.log

k replace --force -f /tmp/ku-tmp.yaml
